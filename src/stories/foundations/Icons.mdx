import { Meta } from '@storybook/blocks';
import { IconsPage } from '../../docs-components/IconsPage';

<Meta title="Foundations/Icons" />

# Icons

Outlined icon set used across Spark components. Icons are grouped by category and shown at both
supported sizes. Search by name or slug and download each size as SVG.

<IconsPage />
