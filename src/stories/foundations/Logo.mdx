import { Meta } from '@storybook/blocks';
import { LogoPage } from '../../docs-components/LogoPage';

<Meta title="Foundations/Logo" />

# Logo

Spark brandmarks — logo-only, text-only, and lockup variants in **black** (light backgrounds) and
**white** (dark backgrounds).

<LogoPage />
